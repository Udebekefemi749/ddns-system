[project]
name = 'ddns-system'
description = ''
authors = []
telemetry = true
cache_dir = './.cache'
requirements = []
[contracts.domain-registry]
path = 'contracts/domain-registry.clar'
clarity_version = 3
epoch = 3.1

[contracts.domain-resolver]
path = 'contracts/domain-resolver.clar'
clarity_version = 3
epoch = 3.1
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
